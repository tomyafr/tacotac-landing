node_modules/
.env
*.log
.DS_Store
Thumbs.db

# Base de données SQLite (données users/usage — jamais versionnée)
*.db
*.db-wal
*.db-shm
*.sqlite

# Tokens TikTok (access/refresh token en clair — jamais versionné)
tiktok-tokens.json
